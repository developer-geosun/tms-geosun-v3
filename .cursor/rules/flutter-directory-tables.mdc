---
description: Flutter-таблиці довідників — еталон /directories/countries
globs:
  - frontend-flutter/lib/features/directories/**
  - frontend-flutter/test/features/directories/**
  - frontend-flutter/lib/**/*paged_table*.dart
alwaysApply: false
---

# Flutter directory tables

Еталон: `/directories/countries`.
Код: `countries_directory_page.dart`, спільний каркас `directory_paged_table.dart` / `directory_table_layout.dart`.
Не копіюй `PaginatedDataTable` — власна таблиця в `Card` через `DirectoryPagedTable`.

## Оболонка сторінки

- Пошук (`hintText`, `maxWidth` 420) + `DirectoryRefreshButton` (`AppElevatedIconButton`).
- `DirectoryLoadProgress`: **завжди** 4 px, щоб таблиця не стрибала; анімація лише під час завантаження.
- Помилки API — `showAppSnack` (`AppSnackKind.error`), не текст на сторінці. Успішні дії (синк тощо) — `AppSnackKind.success`.
- Відступи: 24 px (широкий), 12 px якщо `width < appShellWideBreakpoint` (800).
- Сортування на клієнті всього списку, потім зріз сторінки. Стовпець сортування — **enum**, не `int` індекс.

## Структура таблиці

`Card` → липка шапка → `Expanded` (лише рядки скроляться) → пагінація **внизу картки**, поза `ListView`.

```
❌ PaginatedDataTable / пагінація всередині скролу рядків
✅ шапка фіксована, Scrollbar + ListView.separated лише для body
```

## Кольори (Material 3, ColorScheme)

| Елемент | Токен |
|---|---|
| AppBar | `surface` / `onSurface`, низ — `outlineVariant` |
| Rail selected | `secondaryContainer` (єдине велике peach/accent-заповнення) |
| Шапка таблиці | `surfaceContainer` / `onSurface` |
| Активне сортування | `primary` на підписі й стрілці |
| Парний рядок | `surface` |
| Непарний рядок | `surfaceContainerLow` |
| Hover рядка | `surfaceContainerHigh` |
| Текст шапки | `titleSmall`, weight 600 |

Не хардкодити hex. Не фарбувати AppBar і шапку таблиці в `secondaryContainer`.

## Стовпці та compact

- Широкий екран: усі стовпці. ISO/код — `FixedColumnWidth` + chip; назва поточної мови UI підкреслена (`emphasize`).
- `width < 800`: менше стовпців (країни: ISO-2, ISO-3, **одна** назва; валюти: код, назва, курс).
- Клітинки: `maxLines: 1`, `ellipsis`; `Tooltip` **лише** якщо текст обрізано.
- Тап по шапці: той самий стовпець → інверсія напрямку; інший → `ascending: true`. Активний стовпець — стрілка 16 px, колір `primary`.

## Пагінація

- Опції: `[5, 10, 15, 25, 50]`. За замовчуванням і максимум: **50**.
- `lastRow = min((pageIndex + 1) * rowsPerPage, totalCount)`.
- Якщо всі рядки на одній сторінці — `directoryRecordCount`, без first/prev/next/last.
- Інакше підписи `MaterialLocalizations` (`rowsPerPageTitle`, `pageRowsInfoTitle`, tooltips сторінок).
- Compact: два рядки (`Wrap` розмір+діапазон, потім навігація якщо є). Desktop: один рядок праворуч.

## Заборонено

- З’являти/ховати `LinearProgressIndicator` без резервної висоти.
- Три колонки назви на смартфоні.
- Новий тип таблиці «з нуля», якщо можна повторити цей каркас.
