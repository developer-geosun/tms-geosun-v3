---
description: После выполнения плана, перед коммитом или после значительных правок frontend запускать npm run lint:fix
globs:
  - frontend-angular/src/**/*.ts
  - frontend-angular/src/**/*.html
  - frontend-angular/eslint.config.js
alwaysApply: false
---

# Frontend: ESLint autofix

После **выполнения плана**, перед коммитом или после **значительных** правок frontend применяй ESLint с автофиксом. Prettier в проекте нет; аналог Spotless — `lint:fix`.

Скрипт: `npm run lint:fix` → `ng lint --fix` (`frontend-angular/package.json`, конфиг `frontend-angular/eslint.config.js`). Чинит неиспользуемые импорты, часть stylistic TypeScript и правила, которые ESLint умеет править сам.

## Команда

Из каталога `frontend-angular/`:

```powershell
npm run lint:fix
```

Из корня репозитория:

```powershell
npm --prefix frontend-angular run lint:fix
```

## Когда запускать

- После выполнения плана (все шаги реализации закрыты) — один раз, если в плане менялся `frontend-angular/`.
- Перед `git commit`, если в коммите есть `frontend-angular/src/**/*.ts` или `*.html`.
- После крупного изменения (новая страница, рефакторинг, серия правок по ТЗ) — один раз в конце, не после каждого файла.
- Если `lint:fix` изменил файлы — оставь их в рабочем дереве (не откатывай).

## Когда не запускать

- После мелкой правки (опечатка, одна строка, перевод) — не гоняй lint сразу.
- Не для backend и не вместо Spotless.
- `npm run lint` — только проверка; для сдачи нужен **lint:fix**.
