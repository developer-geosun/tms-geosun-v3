---
description: После выполнения плана, перед коммитом или после значительных правок Java запускать mvn spotless:apply
globs:
  - backend-java/src/**/*.java
  - backend-java/pom.xml
alwaysApply: false
---

# Backend: Spotless

После **выполнения плана**, перед коммитом или после **значительных** правок Java в `backend-java/` применяй Spotless. Не оставляй неприменённый Google Java Format в сдаваемом коде.

Плагин: `com.diffplug.spotless` в `backend-java/pom.xml` (Google Java Format GOOGLE, unused imports, trailing whitespace).

## Команда

Из каталога `backend-java/`:

```powershell
mvn spotless:apply
```

Из корня репозитория:

```powershell
mvn -f backend-java/pom.xml spotless:apply
```

## Когда запускать

- После выполнения плана (все шаги реализации закрыты) — один раз, если в плане менялся `backend-java/`.
- Перед `git commit`, если в коммите есть Java из `backend-java/`.
- После крупного изменения (новая фича, рефакторинг, серия правок по ТЗ) — один раз в конце, не после каждого файла.
- Если `spotless:apply` изменил файлы — оставь их в рабочем дереве (не откатывай).

Перед Spotless (или вместе с финальной проверкой backend): **ReadLints** по изменённым Java-файлам — **0** предупреждений null-safety (16778128). См. правило `java-null-safety.mdc`.

## Когда не запускать

- После мелкой правки (опечатка, одна строка, комментарий) — не гоняй Spotless сразу.
- Не для frontend и не для файлов вне `backend-java/`.
- Не заменяй Spotless ручным переформатированием «на глаз».
- `mvn spotless:check` — только проверка; для сдачи нужен **apply**.
